# 测试报告

执行日期：2026-07-24

## 实际执行

- `node --check heroes.js`、`node --check core.js`、`node --check script.js`；
- `python tests/run_tests.py`；
- 52 名英雄数据结构、ID、中英文名、职责和主/备用路径校验；
- 职责数量：重装 14、输出 24、支援 14；
- 207 张现有 WebP 文件逐一读取并与 `asset_report.json` 中的 SHA-256 比对；
- 52 张主全身照和 52 张备用全身图逐张解码、生成总览并检查原图边界；
- 按 600×800 桌面容器和 328×437 手机容器逐图计算等比缩放，确认 104 张全身图均不会越界或改变比例；
- `core.js` 在 Node.js 中实际执行 120 个随机顺序样本；
- 图标/全身照 × 全部/重装/输出/支援共 8 组完整分配、结果生成和重新导入流程；
- HTML/CSS/JavaScript 静态运行约束检查：相对脚本路径、无 `fetch()`、无外部 CDN、无内联事件、无 `eval`、无框架依赖；
- 打印、分享长图、响应式、通用图片 alt、焦点样式、模态语义、`aria-live` 和 reduced-motion 规则检查。
- 单人模式、全部列表、当前分配表和结果页统一使用原始宽高比完整容纳全身照。

## 核心逻辑覆盖

- Fisher–Yates 不修改输入数组，英雄顺序和名称顺序均为无重复排列；
- 全部、重装、输出、支援筛选数量及名称池范围；
- 一个名称不能同时分配给两个英雄；
- 更换名称后旧名称立即释放；
- 草稿理由、当前索引和占用名称经 JSON 序列化后可以恢复；
- 空白理由不能完成分配；
- 数据签名变化时拒绝恢复旧存档；
- 所有项目完成前不能提交；
- 正确、错误、跳过和排除跳过后的正确率计算；
- 结果 JSON 的必需字段和稳定 ID 对应关系；
- 有效 JSON 导入；
- 重复名称、无效英雄 ID、空白理由、错误版本和畸形 JSON 被拒绝。

测试统计样例：14 位重装英雄中，猜对 11、猜错 2、图片故障跳过 1，已回答 13，正确率 84.6%。

## `file://` 与浏览器级检查

项目的入口、脚本和样式均为相对本地路径，运行时代码不使用 `fetch()`、模块请求或本地 JSON，自动测试确认不依赖服务器。内置浏览器尝试直接打开 `file:///…/overwatch-hero-quiz/index.html?test=1` 时，被当前浏览器安全策略拒绝访问本地文件。

因此以下项目在本环境只能完成代码和结构检查，未能取得真实浏览器渲染证据：

- 直接双击后的实际首屏截图；
- 真实点击每个 DOM 按钮的端到端流程；
- 真实 390px 视口的滚动宽度和浏览器控制台记录；
- 系统打印预览画面。

没有改用本地服务器或其他绕过方式，因为交付目标必须保持直接 `file://` 运行，且浏览器策略禁止绕过本地文件访问限制。

## 最终自动测试结果

`python tests/run_tests.py`：**全部通过**。

现有 `heroes.js`、`asset_report.json`、`tools/download_assets.py` 和 `assets/` 未修改。207 张图片的路径、文件数量和 SHA-256 与修改前报告一致。
